# X OAuth Authorization Fix Test

## 2026-08-17

بعد نشر `social-oauth-callback` بالإصدار 5 مع `verify_jwt=false`، تم فتح تطبيق الإنتاج `https://socialpilot-eta.vercel.app/` من جلسة المستخدم الحالية. التطبيق حمّل بنجاح، والجلسة بقيت قائمة، وظهرت شاشة Dashboard مع مؤشرات المحتوى والحسابات والتنقل إلى «المزيد».

التحقق المباشر السابق من callback بدون Authorization أعاد HTTP 302 إلى التطبيق مع رسالة خطأ مدخلات ناقصة بدل `UNAUTHORIZED_NO_AUTH_HEADER`، ما يثبت أن الطلب يصل إلى كود الوظيفة بدل رفضه من بوابة Supabase.

لم يتم إكمال موافقة X أو إرسال أي منشور أثناء هذا الاختبار.

## واجهة الإنتاج

تم فتح شاشة «المزيد» ثم بطاقة الحسابات من داخل الإنتاج. ظهر فيسبوك وإنستجرام ولينكدإن كحسابات متصلة، وظهر X كحساب غير متصل مع زر «ربط»، وهو السلوك المتوقع بعد إصلاح الحالة.

## نتيجة إعادة الاختبار

بعد الضغط على «ربط» لـ X من الإنتاج، لم تنتقل الصفحة إلى X؛ ظهرت رسالة `Failed to fetch` داخل الواجهة. لم يظهر إخراج مفيد في Console. هذا يثبت أن إصلاح callback أزال خطأ البوابة الخاص بالرجوع، لكن توجد مشكلة مستقلة في استدعاء وظيفة بدء OAuth أو في الإصدار المنشور/ترويسة Authorization لذلك الاستدعاء.

## تشخيص طلب البدء

من جلسة الإنتاج نفسها، تم استدعاء `social-oauth-start` مع JWT موجود. الوظيفة أعادت HTTP 403 برسالة `You do not have access to this workspace` عند استخدام workspace تجريبي غير صحيح، ولم تُعد `UNAUTHORIZED_NO_AUTH_HEADER`. الجلسة تحتوي مستخدمًا حاليًا وJWT صالحًا.

الاستنتاج المرحلي: callback أصبح صحيحًا، لكن رسالة `Failed to fetch` من زر X تحتاج اختبارًا باستخدام workspace الحقيقي أو فحصًا إضافيًا لعقد start في الإصدار المنشور.

## مقارنة حرفية مع Bundle الإنتاج

تم تنزيل bundle الإنتاج الحالي، وثبت أنه يستخدم `functions/v1/social-oauth-start` وليس `x-oauth-connect`. كما تمت محاكاة الاستدعاء حرفيًا مع نفس JWT و`apikey` المضمّنين في bundle، وبـ workspace الحقيقي، فأعاد HTTP 200 ورابط `https://twitter.com/i/oauth2/authorize` مع `state` وPKCE.

لذلك فإن وظيفة البدء وإرسال Authorization تعمل من نفس جلسة الإنتاج. ظهور `Failed to fetch` عند النقر يحتاج إعادة اختبار بعد تحديث/تجاوز cache المتصفح أو فحص event handler أثناء النقر؛ لا يوجد دليل حالي على أن Supabase يرفض Authorization في start.

## Cache-busting

تم فتح `/app/accounts?oauth_fix_test=20260817b` ثم انتظار التحميل. أعاد المسار التطبيق بنجاح، واحتفظ بجلسة المستخدم، وظهرت شاشة «المزيد» قبل فتح بطاقة الحسابات. هذا يستبعد 404 أو فقدان الجلسة كسبب عام.

## النتيجة النهائية على الإنتاج

بعد إعادة تحميل مسار الحسابات مع cache-busting، ظهر X بحالة غير متصل وزر «ربط». عند الضغط، ظهرت حالة «جارٍ التحويل»، ثم انتقل المتصفح إلى:

`https://x.com/i/oauth2/authorize`

مع redirect إلى `social-oauth-callback` ووجود `state` وPKCE (`code_challenge`). صفحة X عرضت: `To use this App you have to be logged in to X.`

هذا يثبت أن خطأ `Missing authorization header` تم إصلاحه عمليًا في callback، وأن بدء OAuth يعمل من الإنتاج. لم يتم تسجيل الدخول إلى X أو منح الصلاحية، لذلك لم يحدث حفظ حساب أو تبادل token.
